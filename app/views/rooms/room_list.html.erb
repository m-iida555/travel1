
<div class="around-line">
  <div class="room-index">
    <h3>登録済みルーム一覧</h3>

  
  <thead>
  <tr>
    <th class="rooms-font">ルーム画像</th>
    <th class="rooms-font">ルーム名</th>
    <th class="rooms-font">ルーム紹介</th>
    <th class="rooms-font">料金</th>
    <th class="rooms-font">作成日</th>
  </tr>
  </thead>
 <tbody>
 <% @rooms.each do |room| %>
  <tr>
    <td><%= room.roompic %></td>
    <td><%= link_to room.roomname, room_path(room.id) %></td> <!-- link_toのパスがshowの時、controllerのshowアクションでparamsからidを拾っていれば、link_toでid指定はいらない。roomだけ　<%#=link_to "編集する",edit_blog_path(blog.id) %>）-->
    <td><%= room.roominfo %></td>
    <td><%= room.money %></td>
    <td><%= room.created_at%></td>
  </tr>
  <br>
  <% end %>
  </tbody>
  

  </div>
</div>