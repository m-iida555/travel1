class SessionsController < ApplicationController
  
  #def create
  #if  #session=User.find(params.require(:email,:password))
    #user && user.authenticate(params[:email,:password])
    #session[:user_id]=user.id
    #redirect_to reservations_hotel_index_path
  #else
    #render"new"
  #end
  #end
  
  #def destroy

  #end
end
